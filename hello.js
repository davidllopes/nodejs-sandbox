const lodash = require("lodash");

let items = ["spoon", "fork", "knife", "vase", "picture"];
let temp = lodash.first(items);

console.log(temp);

let test = lodash.random(0, 55);

console.log(test);
